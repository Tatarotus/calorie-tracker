"# Deterministic Nutrition Ingestion Pipeline\n\nRedesign the food parsing and nutrition resolution architecture from inconsistent heuristics into a highly robust, 11-stage deterministic pipeline.\n\n## User Review Required\n\n> [!IMPORTANT]\n> **API & Macro Hallucination Ban**: The LLM will now function *strictly* as a linguistic parser and semantic validator. It will **never** generate or fabricate macro-nutrition numbers.\n>\n> All nutrition numbers will come strictly from:\n> 1. User manual overrides (authoritative, highest priority)\n> 2. The local nutrition cache\n> 3. FatSecret API\n> 4. Fuzzy fallback reference database\n> SerpAPI will act strictly as a low-confidence emergency fallback, passing through LLM validation.\n\n> [!IMPORTANT]\n> **Normalized DB Architecture**: We will introduce a 3-table normalized schema to completely separate food identity from serving representation and manual overrides:\n> 1. `canonical_foods`: Stable identity layer (`id`, `canonical_name`, `normalized_name`, `aliases_json`, `language`, `category`, `created_at`, `updated_at`).\n> 2. `nutrition_cache`: Resolved serving/nutrition profiles (`id`, `canonical_food_id`, `serving_amount`, `serving_unit`, `calories`, `protein`, `carbs`, `fat`, `fiber`, `source_type`, `source_confidence`, `source_reference`, `resolution_method`, `created_at`, `updated_at`).\n> 3. `user_overrides`: Authoritative override layer (`id`, `canonical_food_id`, `serving_amount`, `serving_unit`, `calories`, `protein`, `carbs`, `fat`, `fiber`, `override_reason`, `created_at`, `updated_at`).\n\n> [!WARNING]\n> **Database Cache Migration**: During startup, a migration will automatically port existing legacy `food_cache` entries into `canonical_foods` and `nutrition_cache` tables with `source_type = 'legacy_system'`, maintaining full data integrity.\n\n---\n\n## Proposed Resolution Flow (11 Stages)\n\nThe ingestion pipeline will execute the following steps in sequence:\n\n```mermaid\ngraph TD\n    Input[Raw User Input] --> Stage1[1. Deterministic Normalization]\
<truncated 5020 bytes>